{
  "name": "nordicriser-marketing-site",
  "version": "1.0.0",
  "private": true,
  "description": "Nordic Riser AB marketing site — static, multi-locale, built with Eleventy",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy --output=docs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
